info:
  name: Teamregels ophalen
  type: http
  seq: 19
  tags:
    - beheer

http:
  method: GET
  url: "{{baseUrl}}/api/beheer/teamregels"
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: lt
      value: "400"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  GET /api/beheer/teamregels

  Teamregels ophalen

  Geeft alle teamregels uit `dbo.TeamRegels`, inclusief inactieve (zodat ze heractiveerbaar zijn).
  Regeltypen: `BufferVoor`, `BufferNa`, `VoorkeurVeld`.


  Spec declares security scheme "functionKey" (apiKey).
