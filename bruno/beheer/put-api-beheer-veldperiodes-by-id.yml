info:
  name: Periode bijwerken
  type: http
  seq: 59
  tags:
    - beheer

http:
  method: PUT
  url: "{{baseUrl}}/api/beheer/veldperiodes/:id"
  params:
    - name: id
      value: "1"
      type: path
  body:
    type: json
    data: |-
      {
        "naam": "Zomerstop",
        "datumVan": "1970-01-01",
        "datumTot": "1970-01-01",
        "actief": true
      }
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: lt
      value: "400"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  PUT /api/beheer/veldperiodes/{id}

  Periode bijwerken

  Spec declares security scheme "functionKey" (apiKey).

  Some values below were invented; check them before sending.
