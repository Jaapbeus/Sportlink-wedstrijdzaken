info:
  name: Veldbeschikbaarheid verwijderen
  type: http
  seq: 5
  tags:
    - beheer
    - destructive

http:
  method: DELETE
  url: "{{baseUrl}}/api/beheer/veldbeschikbaarheid/:id"
  params:
    - name: id
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  DELETE /api/beheer/veldbeschikbaarheid/{id}

  Veldbeschikbaarheid verwijderen

  Spec declares security scheme "functionKey" (apiKey).

  Some values below were invented; check them before sending.
