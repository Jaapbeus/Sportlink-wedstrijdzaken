info:
  name: Veld bijwerken
  type: http
  seq: 49
  tags:
    - beheer

http:
  method: PUT
  url: "{{baseUrl}}/api/beheer/velden/:veldNummer"
  params:
    - name: veldNummer
      value: "1"
      type: path
  body:
    type: json
    data: |-
      {
        "veldNaam": "string",
        "veldType": "string",
        "heeftKunstlicht": false,
        "actief": false
      }
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: lt
      value: "400"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  PUT /api/beheer/velden/{veldNummer}

  Veld bijwerken

  Wijzigt naam, type, kunstlicht of activeert/deactiveert een veld.

  Spec declares security scheme "functionKey" (apiKey).

  Some values below were invented; check them before sending.
