info:
  name: Club-instellingen ophalen
  type: http
  seq: 13
  tags:
    - beheer

http:
  method: GET
  url: "{{baseUrl}}/api/beheer/settings"
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: lt
      value: "400"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  GET /api/beheer/settings

  Club-instellingen ophalen

  Leest niet-gevoelige AppSettings velden. SportlinkClientId en geheimen worden nooit
  teruggegeven. Voegt een leesbare CRON-preview toe als FetchSchedule aanwezig is.


  Spec declares security scheme "functionKey" (apiKey).
