name: TMP vars case test
on:
  pull_request:
    branches: [develop]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Print vars.DatabaseTier
        run: echo "value=[${{ vars.DatabaseTier }}]"
